galaxy_info:
  author: Muhammad Munawar Raza
  designation: Sr. DevOps Engineer

  # About: This role is used to update the servers to patch the vulnerabilites.
  # use following command to execute this playbook
  # ansible-playbook -i inventory_file update_linux_servers.yml -e target=target_hosts --become --ask-become-pass
  

  license: license (GPL-2.0-or-later, MIT, etc)

  min_ansible_version: 2.1

  platforms:
  - name: RedHat
    versions:
    - all

  galaxy_tags: []
dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
