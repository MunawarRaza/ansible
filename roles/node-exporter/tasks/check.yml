---
- name: Check node_exporter binary
  stat:
    path: "{{ node_exporter_binary }}"
  register: node_binary


- name: Display installation details
  debug:
    msg:
      - "======================================"
      - "details: {{ node_binary.stat }}"
      # - "Node Exporter Installed Successfully"
      # - "Path    : {{ node_exporter_binary }}"
      # - "Version : {{ final_version.stdout_lines[0] }}"
      # - "Service : {{ ansible_facts.services.get(node_exporter_service, {}).get('state', 'Not Installed') }}"
      - "======================================"


# - name: Get installed version
#   command: "{{ node_exporter_binary }} --version"
#   register: node_version
#   changed_when: false
#   failed_when: false
#   when: node_binary.stat.exists

# - name: Extract installed version
#   set_fact:
#     installed_version: "{{ node_version.stdout | regex_search('version ([0-9.]+)', '\\1') | first }}"
#   when: node_binary.stat.exists

# - name: Determine install status
#   set_fact:
#     install_required: "{{ not node_binary.stat.exists }}"
#     upgrade_required: "{{ node_binary.stat.exists and installed_version != node_exporter_version }}"

# - name: Upgrade required
#   set_fact:
#     install_required: true
#   when: upgrade_required

# - name: Check service file
#   stat:
#     path: "{{ node_exporter_service_file }}"
#   register: service_file

# - name: Gather service facts
#   service_facts:

# - name: Check service status
#   set_fact:
#     service_running: >-
#       {{
#         node_exporter_service in ansible_facts.services and
#         ansible_facts.services[node_exporter_service].state == "running"
#       }}